name: tcp_tunnel_agent
description: TCP Tunnel Agent — connects to relay server and forwards TCP traffic to work resources
version: 1.0.62

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web_socket_channel: ^3.0.1
  args: ^2.5.0
  uuid: ^4.4.0
  logging: ^1.2.0
  path: ^1.9.0
